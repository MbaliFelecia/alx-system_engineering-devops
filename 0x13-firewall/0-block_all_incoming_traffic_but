# Check the status of ufw 
sudo ufw status

# If ufw is not active, you can enable it by:
suo ufw enable

# Set the default incoming policy to "deny" to block all incoming traffic by:
sudo ufw default deny incomming

# Set the default outgoing policy to "allow" to allow  all outgoing traffic by:
sudo ufw default allow outgoing

# Allow incoming traffic on TCP ports 22, 443, and 80 by:

sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# Verify that the rules have been added by:

sudo ufw show added

# Enable ufw by:

sudo ufw enable
